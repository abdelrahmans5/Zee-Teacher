# Frontend (React + Vite)

## Run
```bash
cd frontend
cp .env.example .env
npm install
npm run dev
```

Then open: http://127.0.0.1:5173

## Env
- `VITE_API_BASE_URL`: backend URL (default local FastAPI).
